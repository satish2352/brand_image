<IfModule mod_rewrite.c>
    RewriteEngine On

    # Skip if already in public/
    RewriteCond %{REQUEST_URI} ^/brand_image/public [NC]
    RewriteRule ^ - [L]

    # Rewrite all requests to public/ (preserving path for static files)
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
